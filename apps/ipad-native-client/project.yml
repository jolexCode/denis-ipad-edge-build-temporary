name: DenisVoice
options:
  bundleIdPrefix: so.denis.voice
  deploymentTarget:
    iOS: "26.0"
  xcodeVersion: "26.0"

settings:
  base:
    SWIFT_VERSION: "6.0"
    TARGETED_DEVICE_FAMILY: "2"
    INFOPLIST_FILE: Sources/DenisVoice/Info.plist
    PRODUCT_NAME: Denis
    PRODUCT_BUNDLE_IDENTIFIER: so.denis.voice.client
    IPHONEOS_DEPLOYMENT_TARGET: "26.0"

packages:
  Starscream:
    url: https://github.com/daltoniam/Starscream
    from: 4.0.6

targets:
  DenisVoice:
    type: application
    platform: iOS
    sources:
      - Sources/DenisVoice
    dependencies:
      - package: Starscream
      - sdk: Foundation.framework
      - sdk: UIKit.framework
      - sdk: SwiftUI.framework
      - sdk: AVFoundation.framework
      - sdk: CoreMedia.framework
      - sdk: CoreVideo.framework
      - sdk: CoreImage.framework
      - sdk: Accelerate.framework
      - sdk: Speech.framework
      - sdk: Metal.framework
      - sdk: CoreML.framework
      - sdk: LocalAuthentication.framework
      - sdk: Vision.framework
      - sdk: FoundationModels.framework
      - sdk: NaturalLanguage.framework
      - sdk: SoundAnalysis.framework
      - sdk: Network.framework
      - sdk: BackgroundTasks.framework
      - sdk: AppIntents.framework
    settings:
      base:
        INFOPLIST_FILE: Sources/DenisVoice/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: so.denis.voice.client
        CODE_SIGN_STYLE: Manual
        CODE_SIGN_IDENTITY: ""
        DEVELOPMENT_TEAM: ""
    info:
      path: Sources/DenisVoice/Info.plist
    entitlements:
      path: Sources/DenisVoice/DenisVoice.entitlements
    preBuildScripts:
      - name: SwiftLint
        script: |
          if which swiftlint >/dev/null; then swiftlint; fi
        basedOnDependencyAnalysis: false
